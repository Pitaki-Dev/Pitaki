# TypeScript 7 的原生编译器（tsgo/tsc）用「可执行文件自身所在目录」定位 lib.*.d.ts。
# pnpm 默认用硬链接把二进制从内容寻址的 store 链接进 node_modules，
# 导致可执行文件自解析到 store 路径（那里没有 lib.d.ts），启动即 panic：
#   panic: bundled: /…/pnpm/store/v3/files/4b/lib.d.ts does not exist; this executable may be misplaced
# 用 copy 导入方式即可解决（代价是安装稍慢、占用更多磁盘）。
package-import-method=copy
